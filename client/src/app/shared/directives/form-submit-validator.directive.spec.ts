import { FormSubmitValidatorDirective } from './form-submit-validator.directive';

describe('FormSubmitValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FormSubmitValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
